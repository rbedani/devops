# SDD Configuration — devops
# Auto-generated by sdd-init | hybrid mode | 2026-07-24

context: |
  Framework Browser Jobs — Playwright-based job listing scraper with SQLite
  storage and Telegram alerts. Python 3.11+, setuptools build.

strict_tdd: true

testing:
  runner:
    command: pytest
    framework: pytest
    options: -v --tb=short
    layers:
      unit:     { available: true,  tool: "pytest (unittest-style)" }
      integration: { available: false, tool: "pytest (stub only)" }
      e2e:      { available: false, tool: "pytest (stub only)" }
  coverage:
    available: true
    command: pytest --cov=src
  quality:
    linter:       { available: true,  command: "ruff check ." }
    type_checker: { available: true,  command: "mypy src/" }
    formatter:    { available: false, command: null }

phases:
  explore: { skip_gate: false }
  propose: { skip_gate: false }
  spec:    { skip_gate: false }
  design:  { skip_gate: false }
  tasks:   { skip_gate: false }
  apply:   { skip_gate: false }
  verify:  { skip_gate: false }
  archive: { skip_gate: false }

review_budget: 400
delivery_strategy: auto-forecast